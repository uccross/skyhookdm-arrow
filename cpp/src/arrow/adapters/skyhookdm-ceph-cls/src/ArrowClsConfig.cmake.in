@PACKAGE_INIT@

include(CMakeFindDependencyMacro)
find_dependency(Arrow)

# Load targets only once. If we load targets multiple times, CMake reports
# already existent target error.
if(NOT (TARGET arrow_cls_shared OR TARGET arrow_cls_static))
  include("${CMAKE_CURRENT_LIST_DIR}/ArrowClsTargets.cmake")
endif()
